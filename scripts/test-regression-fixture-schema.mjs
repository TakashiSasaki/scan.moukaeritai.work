import fs from 'node:fs';import path from 'node:path';import Ajv from 'ajv';import addFormats from 'ajv-formats';import {fileURLToPath} from 'node:url';
const root=path.resolve(path.dirname(fileURLToPath(import.meta.url)),'..'), dir=path.join(root,'contracts/fixtures/regression'); const ajv=new Ajv({allErrors:true,strict:false}); addFormats(ajv); const validate=ajv.compile(JSON.parse(fs.readFileSync(path.join(dir,'fixture.schema.json'),'utf8'))); for(const file of fs.readdirSync(dir).filter(f=>f.endsWith('.json')&&f!=='fixture.schema.json')){const data=JSON.parse(fs.readFileSync(path.join(dir,file),'utf8')); if(!validate(data)) throw new Error(`${file}: ${ajv.errorsText(validate.errors)}`);} console.log('Regression fixture schema validation passed.');
