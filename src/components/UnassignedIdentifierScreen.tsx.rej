--- src/components/UnassignedIdentifierScreen.tsx
+++ src/components/UnassignedIdentifierScreen.tsx
@@ -143,7 +137,11 @@
         });

         await batch.commit();
-        toast.success('Identifier attached.');
+        if (validation.isIdempotent) {
+            toast.success('Identifier is already attached to this object.');
+        } else {
+            toast.success('Identifier attached.');
+        }
         navigate(`/object/${objectId}`);
     } catch (err: any) {
         toast.error(err.message || 'Failed to attach identifier');
