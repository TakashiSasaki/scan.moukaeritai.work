name: Dogfood agent-policy adoption

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  hold:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Prepared adoption files are being assembled on the branch."
